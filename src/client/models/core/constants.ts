export enum ClientDocumentEvents {
  documentContentUpdate,
}

export enum ShareDBDocStatus {
  Loading = 'Loading',
  Loaded = 'Loaded',
  LoadFailed = 'LoadFailed',
  Error = 'error',
}

export enum UserStatus {
  NotLoggedIn = 'Not Logged in',
  LoggedIn = 'Logged in',
}
